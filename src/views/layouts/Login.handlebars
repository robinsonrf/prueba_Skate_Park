<!DOCTYPE html>
<html lang="en">
  {{>Head}}
  <body>
    {{>Nav}}
    <div class="row container m-auto pt-5 mb-5 m-top">
      <div class="col-sm-6 py-5">
      <h2>Iniciar Sesión</h2>
      <hr class="w-75 m-auto my-2" />

      <form>
        <div class="form-group">
          <div class="form-group">
            <label>Email</label>
            <input class="form-control w-50 m-auto" />
          </div>
          <div class="form-group">
            <label>Password</label>
            <input class="form-control w-50 m-auto" />
          </div>
        </div>
        <button class="btn btn-success mt-2 mb-3">Ingresar</button>
        <p>¿Aún no tienes cuenta? <a href="/registro">Regístrate</a></p>
      </form>
      </div>
    </div>
    {{>Footer}}
    {{>CdnBody}}
  </body>
</html>
